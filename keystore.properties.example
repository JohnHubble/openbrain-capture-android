# Copy to keystore.properties and fill in your values.
# keystore.properties is gitignored - never commit the real one.
storeFile=openbrain-release.keystore
storePassword=CHANGE_ME
keyAlias=openbrain
keyPassword=CHANGE_ME
